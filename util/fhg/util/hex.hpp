#ifndef FHG_UTIL_HEX_HPP
#define FHG_UTIL_HEX_HPP

#include <string>

namespace fhg
{
  namespace util
  {
    std::string to_hex (std::string const &);
    std::string from_hex (std::string const &);
  }
}

#endif
