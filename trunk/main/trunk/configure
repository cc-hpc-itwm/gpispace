#! /bin/sh
# 
# Bootstrap the project from a clean checkout.
#
if [ -z $BUILD_DIR ]; then
    BUILD_DIR = "build"
fi

if test ! -d "${BUILD_DIR}"; then
	mkdir -p "${BUILD_DIR}"
fi

cd "${BUILD_DIR}" && cmake .. $@
