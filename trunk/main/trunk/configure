#! /bin/sh
# 
# Bootstrap the project from a clean checkout.
#
build_dir="$BUILD_DIR"
if [ -n $build_dir ]; then
    build_dir="build"
fi

if test ! -d "${build_dir}"; then
	mkdir -p "${build_dir}"
fi

cd "${build_dir}" && cmake .. $@
