#ifndef WE_TESTS_MAP_REDUCE_EXAMPLE_HPP
#define WE_TESTS_MAP_REDUCE_EXAMPLE_HPP 1

namespace we {
  namespace tests {
    template <typename Activity, typename Transition>
    struct map_reduce
    {
      static Activity generate()
      {
        Activity act;
        return act;
      }
    };
  }
}

#endif
