# mirko.rahn@itwm.fraunhofer.de

GENERATOR="@CMAKE_CURRENT_BINARY_DIR@/gen"
MK_USER_PARAMETER="@CMAKE_CURRENT_BINARY_DIR@/mk_user_parameter"
INPUT_FILE="${DIR_STATE}/input"
OUTPUT_FILE="${DIR_STATE}/output"
VERIFY_FILE="${DIR_STATE}/verify"

size_input=$((20*2**20))
size_output=$((20*2**20))
size_block=$((2**20))
num_block=$((120))
size=$((num_block * size_block))
user_data="$("${MK_USER_PARAMETER}" \
             --input "${INPUT_FILE}" --output "${OUTPUT_FILE}" --size ${size})"
"${GENERATOR}" --data "${INPUT_FILE}" --verify "${VERIFY_FILE}" --size ${size}

touch "${OUTPUT_FILE}"
