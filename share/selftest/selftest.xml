<defun name="selftest">
  <in name="challenge" type="string" />
  <out name="response" type="string" />

  <module name="selftest" function="selftest ( challenge
				             , response
                                             )"
          pass_context="true">
    <cinclude href="fhglog/LogMacros.hpp"/>
    <cxx flag="-std=c++11"/>
    <code><![CDATA[
        LLOG (INFO, _pnetc_context->logger(), "SDPA selftest");

        LLOG (INFO, _pnetc_context->logger(), "===> challenge := " << challenge);

        response = "sdpa.response." + challenge;

        LLOG (INFO, _pnetc_context->logger(), "===> response := " << response);
    ]]></code>
  </module>
</defun>
