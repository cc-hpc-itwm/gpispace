<defun>
  <in name="amount" type="long" place="amount"/>
  <in name="offset" type="long" place="offset"/>
  <out name="out" type="VOLUME" place="out"/>

  <net>
    <place name="amount" type="long"/>
    <place name="offset" type="long"/>
    <place name="out" type="VOLUME"/>
    <place name="pair" type="pair_long_long"/>

    <include-template href="tagged_sequence.xml"/>

    <specialize name="tag_seq" use="tagged_sequence">
      <type-map replace="T" with="long"/>
      <type-map replace="PAIR" with="pair_long_long"/>
      <type-get name="pair_long_long"/>
    </specialize>

    <transition name="tag_seq" inline="true">
      <use name="tag_seq"/>
      <connect-in port="tag" place="offset"/>
      <connect-in port="amount" place="amount"/>
      <connect-out port="pair" place="pair"/>
    </transition>

    <transition name="translate">
      <defun>
        <in name="pair" type="pair_long_long"/>
        <out name="volume" type="VOLUME"/>
        <expression>
          ${volume.id} := ${pair.id};
          ${volume.offset} := ${pair.tag};
        </expression>
      </defun>
      <connect-in port="pair" place="pair"/>
      <connect-out port="volume" place="out"/>
    </transition>
</net>
</defun>
