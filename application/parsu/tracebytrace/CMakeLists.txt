
set (PARSU_TRACEBYTRACE "${SDPA_APPS_DIR}/parsu/tracebytrace")

add_subdirectory (xml)

set (PARSU_TRACEBYTRACE_DIR "\${SDPA_HOME}/${PARSU_TRACEBYTRACE}")
set (PARSU_PNETC_LIB_DIR "\${SDPA_HOME}/share/sdpa/xml/lib")
