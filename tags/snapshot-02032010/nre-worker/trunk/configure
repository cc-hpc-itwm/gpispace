#! /bin/sh
# 
# Bootstrap the project from a clean checkout.
#

case "$1" in
  -h|--help)
    echo "usage: $0 [-h|--help] -DCMAKE_BUILD_TYPE=Debug other-cmake-options"
    exit 1
    ;;
esac

if test ! -d "build"; then
	mkdir build
fi
cd build && cmake .. ${CMAKE_CFG_DEFAULT_OPTS} $@
