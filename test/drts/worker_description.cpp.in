// Copyright (C) 2021,2023-2026 Fraunhofer ITWM
// SPDX-License-Identifier: GPL-3.0-or-later

#include <boost/test/unit_test.hpp>

#include <gspc/drts/worker_description.hpp>
#include <gspc/drts/private/startup_and_shutdown.hpp>
#include <gspc/drts/private/worker_description_implementation.hpp>

#include <gspc/util/join.hpp>
#include <gspc/util/print_container.hpp>
#include <gspc/util/read_lines.hpp>
#include <gspc/testing/random.hpp>

#include <boost/test/data/test_case.hpp>

#include <gspc/util/join.formatter.hpp>
#include <gspc/util/fmt/std/optional.formatter.hpp>
#include <fmt/core.h>
#include <iostream>
#include <sstream>
#include <tuple>
#include <vector>

namespace gspc
{
  std::ostream& operator<< (std::ostream&, worker_description const&);
  std::ostream& operator<< (std::ostream& os, worker_description const& wd)
  {
    return os
      << "WorkerDescription"
      << "["
      << "capabilities = " << gspc::util::print_container ("[", ", ", "]", wd._->capabilities)
      << ", num_per_node = " << wd._->num_per_node
      << ", max_nodes = " << wd._->max_nodes
      << ", shm_size = " << wd._->shm_size
      << ", socket = " << wd._->socket
      << ", base_port = " << wd._->base_port
      << "]"
      ;
  }
  bool operator== (worker_description const&, worker_description const&);
  bool operator== (worker_description const& lhs, worker_description const& rhs)
  {
#define ESSENCE(wd)                             \
    std::tie ( wd._->capabilities               \
             , wd._->num_per_node               \
             , wd._->max_nodes                  \
             , wd._->shm_size                   \
             , wd._->socket                     \
             , wd._->base_port                  \
             )

    return ESSENCE (lhs) == ESSENCE (rhs);

#undef ESSENCE
  }
}

namespace
{
  struct RandomIdentifier
  {
    std::string operator()() const
    {
      return _random_string
        (gspc::testing::random<std::string>::identifier());
    }

  private:
    gspc::testing::random<std::string> _random_string;
  };

  std::vector<std::string> random_identifiers (std::size_t n)
  {
    using gspc::testing::randoms;
    return randoms<std::vector<std::string>, RandomIdentifier> (n);
  }
  std::vector<std::string> random_identifiers()
  {
    using gspc::testing::random;
    return random_identifiers (random<std::size_t>{} (100, 1));
  }
}

namespace fhg
{
  namespace drts
  {
    BOOST_AUTO_TEST_CASE (plain_identifier_is_capability)
    {
      auto const identifiers (random_identifiers (1));
      gspc::worker_description const wd (identifiers.front());
      gspc::worker_description const expected
        { identifiers
        , 1
        , 0
        , 0
        , std::nullopt
        , std::nullopt
        };

      BOOST_REQUIRE_EQUAL (wd, expected);
    }

    BOOST_AUTO_TEST_CASE (multiple_identifiers_separated_by_PLUS_are_capabilities)
    {
      auto const identifiers (random_identifiers());
      auto const input {fmt::format ("{}", gspc::util::join (identifiers, "+"))};
      gspc::worker_description const wd (input);
      gspc::worker_description const expected
        { identifiers
        , 1
        , 0
        , 0
        , std::nullopt
        , std::nullopt
        };

      BOOST_REQUIRE_EQUAL (wd, expected);
    }

    BOOST_AUTO_TEST_CASE (test_issue_986)
    {
      auto const identifiers
        (gspc::util::read_lines (std::filesystem::path {"@ISSUE_986_DATA@"}));
      auto const input {fmt::format ("{}", gspc::util::join (identifiers, "+"))};
      gspc::worker_description const wd (input);
      gspc::worker_description const expected
        { identifiers
        , 1
        , 0
        , 0
        , std::nullopt
        , std::nullopt
        };

      BOOST_REQUIRE_EQUAL (wd, expected);
    }

    BOOST_AUTO_TEST_CASE (socket_is_separated_from_identifiers_by_HASH)
    {
      auto const identifiers (random_identifiers());
      auto const socket (gspc::testing::random<std::size_t>{}());
      auto const input
        {fmt::format ("{}#{}", gspc::util::join (identifiers, "+"), socket)};
      gspc::worker_description const wd (input);
      gspc::worker_description const expected
        { identifiers
        , 1
        , 0
        , 0
        , std::make_optional (socket)
        , std::nullopt
        };

      BOOST_REQUIRE_EQUAL (wd, expected);
    }

    BOOST_AUTO_TEST_CASE
      (per_node_is_separated_from_identifiers_by_DOUBLE_COLON)
    {
      auto const identifiers (random_identifiers());
      auto const per_node (gspc::testing::random<std::size_t>{}());
      auto const input
        {fmt::format ("{}:{}", gspc::util::join (identifiers, "+"), per_node)};
      gspc::worker_description const wd (input);
      gspc::worker_description const expected
        { identifiers
        , per_node
        , 0
        , 0
        , std::nullopt
        , std::nullopt
        };

      BOOST_REQUIRE_EQUAL (wd, expected);
    }

    BOOST_AUTO_TEST_CASE (per_node_is_separated_from_socket_by_DOUBLE_COLON)
    {
      auto const identifiers (random_identifiers());
      auto const socket (gspc::testing::random<std::size_t>{}());
      auto const per_node (gspc::testing::random<std::size_t>{}());
      auto const input
        { fmt::format ( "{}#{}:{}"
                      , gspc::util::join (identifiers, "+")
                      , socket
                      , per_node
                      )
         };
      gspc::worker_description const wd (input);
      gspc::worker_description const expected
        { identifiers
        , per_node
        , 0
        , 0
        , std::make_optional (socket)
        , std::nullopt
        };

      BOOST_REQUIRE_EQUAL (wd, expected);
    }

    BOOST_AUTO_TEST_CASE (max_nodes_is_separated_from_per_node_by_X)
    {
      auto const identifiers (random_identifiers());
      auto const per_node (gspc::testing::random<std::size_t>{}());
      auto const max_nodes (gspc::testing::random<std::size_t>{}());
      auto const input
        { fmt::format ( "{}:{}x{}"
                      , gspc::util::join (identifiers, "+")
                      , per_node
                      , max_nodes
                      )
         };
      gspc::worker_description const wd (input);
      gspc::worker_description const expected
        { identifiers
        , per_node
        , max_nodes
        , 0
        , std::nullopt
        , std::nullopt
        };

      BOOST_REQUIRE_EQUAL (wd, expected);
    }

    BOOST_AUTO_TEST_CASE (shm_size_is_separated_from_per_node_by_COMMA)
    {
      auto const identifiers (random_identifiers());
      auto const per_node (gspc::testing::random<std::size_t>{}());
      auto const shm_size (gspc::testing::random<std::size_t>{}());
      auto const input
        { fmt::format ( "{}:{},{}"
                      , gspc::util::join (identifiers, "+")
                      , per_node
                      , shm_size
                      )
         };
      gspc::worker_description const wd (input);
      gspc::worker_description const expected
        { identifiers
        , per_node
        , 0
        , shm_size
        , std::nullopt
        , std::nullopt
        };

      BOOST_REQUIRE_EQUAL (wd, expected);
    }

    BOOST_AUTO_TEST_CASE (port_is_separated_from_per_node_by_SLASH)
    {
      auto const identifiers (random_identifiers());
      auto const per_node (gspc::testing::random<std::size_t>{}());
      auto const port (gspc::testing::random<unsigned short>{}());
      auto const input
        { fmt::format ( "{}:{}/{}"
                      , gspc::util::join (identifiers, "+")
                      , per_node
                      , port
                      )
         };
      gspc::worker_description const wd (input);
      gspc::worker_description const expected
        { identifiers
        , per_node
        , 0
        , 0
        , std::nullopt
        , std::make_optional (port)
        };

      BOOST_REQUIRE_EQUAL (wd, expected);
    }

    namespace
    {
      std::vector<bool> onoff()
      {
        return {true, false};
      }
    }

    BOOST_DATA_TEST_CASE
      ( worker_description_parser_optional_combinations
      , onoff() * onoff() * onoff() * onoff()
      , _socket
      , _max_nodes
      , _shm_size
      , _port
      )
    {
      auto const identifiers (random_identifiers());
      auto const socket (gspc::testing::random<std::size_t>{}());
      auto const per_node (gspc::testing::random<std::size_t>{}());
      auto const max_nodes (gspc::testing::random<std::size_t>{}());
      auto const shm_size (gspc::testing::random<std::size_t>{}());
      auto const port (gspc::testing::random<unsigned short>{}());

      std::ostringstream input;
      input << gspc::util::join (identifiers, "+");

      if (_socket)    { input << '#' << socket;    }
                        input << ':' << per_node;
      if (_max_nodes) { input << 'x' << max_nodes; }
      if (_shm_size)  { input << ',' << shm_size;  }
      if (_port)      { input << '/' << port;      }

      gspc::worker_description const wd (input.str());
      gspc::worker_description const expected
        { identifiers
        , per_node
        , _max_nodes ? max_nodes : 0
        , _shm_size ? shm_size : 0
        , _socket ? std::make_optional (socket) : std::nullopt
        , _port ? std::make_optional (port) : std::nullopt
        };
    }

    BOOST_AUTO_TEST_CASE (construction_from_to_string_is_identity)
    {
      using gspc::testing::random;
      auto random_switch (random<bool>{});
      auto random_num (random<std::size_t>{});
      auto random_port (random<unsigned short>{});

      auto const identifiers (random_identifiers());
      auto const _socket (random_switch());
      auto const _per_node (random_switch());
      auto const _max_nodes (_per_node && random_switch());
      auto const _shm_size (_per_node && random_switch());
      auto const _port (_per_node && random_switch());

      gspc::worker_description const wd
        ( identifiers
        , _per_node ? random_num() : 1
        , _max_nodes ? random_num() : 0
        , _shm_size ? random_num() : 0
        , _socket ? std::make_optional (random_num()) : std::nullopt
        , _port ? std::make_optional (random_port()) : std::nullopt
        );

      BOOST_REQUIRE_EQUAL (wd, gspc::worker_description (to_string (wd)));
    }
  }
}
