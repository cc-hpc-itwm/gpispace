# alexander.petry@itwm.fraunhofer.de

source "@CMAKE_CURRENT_BINARY_DIR@/error.sh"
source "@CMAKE_CURRENT_BINARY_DIR@/die.sh"

DIR_STATE=$(mktemp -d -p "$HOME")                           || die $EX_ERR_OTHER

echo "DIR_STATE = ${DIR_STATE}"                             || die $EX_ERR_OTHER

cleanup_dir_state()
{
  rm -rf "${DIR_STATE}"
}
