#include "serve.hpp"

#include <gspc/net/server.hpp>

namespace gspc
{
  namespace net
  {
    server_t *serve (const char * url, server::queue_manager_t &qmgr)
    {
      return (server_t*)(0);
    }
  }
}
