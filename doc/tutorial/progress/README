Progress Example
================

this requires a drts environment and will not work with we-exec (use make submit)!

start sdpa with:
	$ $SDPA_HOME/libexec/sdpa/scripts/start-sdpa -S ~/.sdpa/state -C PROGRESS:1x1 -C WORK:4
