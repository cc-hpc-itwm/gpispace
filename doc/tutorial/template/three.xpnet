<template name="three">
  <template-parameter type="T"/>
  <defun>
    <in name="x" type="T"/>
    <out name="a" type="T"/>
    <out name="b" type="T"/>
    <out name="c" type="T"/>

    <expression>
      ${a} := ${x}; ${b} := ${x}; ${c} := ${x}
    </expression>
  </defun>
</template>
