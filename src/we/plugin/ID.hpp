#pragma once

#include <util-generic/hard_integral_typedef.hpp>

namespace gspc
{
  namespace we
  {
    namespace plugin
    {
      FHG_UTIL_HARD_INTEGRAL_TYPEDEF (ID, unsigned long);
    }
  }
}

FHG_UTIL_HARD_INTEGRAL_TYPEDEF_HASH (gspc::we::plugin::ID)
