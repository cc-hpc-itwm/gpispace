#pragma once

#include <list>
#include <string>

namespace pnet
{
  namespace type
  {
    namespace value
    {
      namespace path
      {
        std::string join (const std::list<std::string>&);
      }
    }
  }
}
