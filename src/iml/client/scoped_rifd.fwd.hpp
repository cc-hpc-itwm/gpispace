#pragma once

namespace iml_client
{
  namespace iml_rifd
  {
    struct strategy;
    struct hostnames;
    struct hostname;
    struct port;
  }

  class scoped_rifd;
  class scoped_rifds;
  class rifds;
}
